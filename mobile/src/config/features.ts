/**
 * Feature flags — set to false to hide a feature from the UI without removing any code.
 * Flip to true to re-enable.
 */
export const FEATURES = {
  /** Login Activity & Active Sessions screen — temporarily hidden */
  SHOW_LOGIN_ACTIVITY: false,
};
